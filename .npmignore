# Python bytecode is a local build artifact: machine-specific and never
# part of the published contract. It reached the 0.19.0 tarball because
# running the test suite compiles the shipped helper in place.
__pycache__/
*.pyc
