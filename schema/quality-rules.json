{
  "$schema-version": "1.17",
  "_comment": "Shared quality minimums, canonical in learn-content-engine and applied by its validateLessonQuality (engine#185). A lesson's purpose lifts one of them: bridge the exercise minimum, quiz the exercise-type minimum. Consumers read these numbers from the pinned engine release instead of keeping a copy.",
  "rules": {
    "minExerciseTypes": 2,
    "minExercisesPerLesson": 5,
    "minFreeTextAccepts": 2,
    "minMatchingPairs": 3,
    "minTheorySteps": 1
  }
}
